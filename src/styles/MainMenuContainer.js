import styled from 'styled-components'

export const MainMenuContainer = styled.nav`
    ul {
      list-style: none;
      padding: 0;
      li{
        list-style: none;
        display: inline;
        margin-right: 10px;
        cursor: pointer;
      }
    }
`